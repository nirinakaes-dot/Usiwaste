export default function Footer() {
  return (
    <footer style={{ textAlign: "center", padding: "24px", color: "var(--text-dim)", fontSize: 13 }}>
      usiwaste — rescue food before closing time
    </footer>
  );
}
