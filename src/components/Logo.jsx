// The mustard "LOGO" block from the Figma. Swap the text for an <img> when
// the real logo asset exists.
export default function Logo() {
  return (
    <div className="mx-auto mb-8 flex h-14 w-36 items-center justify-center rounded-md bg-brand font-semibold tracking-wide text-white">
      LOGO
    </div>
  );
}
